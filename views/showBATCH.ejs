<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
      <style>
        /* General Body */
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f4f6f8;
            color: #333;
            margin: 0;
            padding: 20px;
        }

        h3 {
            color: #2c3e50;
            margin-bottom: 15px;
        }

        /* Forms and Buttons */
        form {
            display: inline-block;
            margin: 10px 10px 20px 0;
        }

        button {
            background-color: #3498db;
            color: white;
            border: none;
            padding: 10px 20px;
            margin: 5px 0;
            border-radius: 5px;
            cursor: pointer;
            font-size: 16px;
            transition: 0.3s;
        }

        button:hover {
            background-color: #2980b9;
        }

        a {
            text-decoration: none;
        }

        /* Student List */
        ul {
            list-style-type: none;
            padding: 0;
        }

        li {
            background-color: #ecf0f1;
            padding: 10px;
            margin-bottom: 8px;
            border-radius: 5px;
            transition: 0.3s;
        }

        li:hover {
            background-color: #d0d7de;
        }

        p {
            font-style: italic;
            color: #7f8c8d;
        }

        /* Responsive */
        @media (max-width: 600px) {
            button {
                width: 100%;
                font-size: 14px;
            }

            form {
                display: block;
            }
        }
    </style>
</head>
<body>
    <% batches.forEach(b => { %>
  <a href="/batch/<%= dept._id%>/batches?batch=<%= b %>">
    <button><%= b %></button>
  </a>
 
<% }) %>

<% if (selectedBatch) { %>
  <h3>Student List (Batch <%= selectedBatch %>)</h3> 

  <% if (stu.length > 0) { %>
    <ul>
      <% stu.forEach(s => { %>
        <li>
     
<b>first name</b><%= s.studFname %> <b>second name </b> <%= s.studSname %> <b>stud email</b>
        <%= s.studEmail%> <b>stud department</b> <%= s.studDept%> <b>study batch </b><%= s.studyBatch%>
           <form action="/deptt/<%= s._id%>/edit" method="get">
      <button>✏️</button>
    </form>
        <form action="/deptt/<%= dept._id%>/<%= s._id%>/deletee" method="post">
            <button type="submit" onclick="return confirm('are you sure to delete this user')">delete</button>
        </form>
        <form action="/assigncourse/<%= dept._id%>/<%= s._id%>/" method="get">
    <button type="submit">assign course</button>
</form>

 <form action="/assigncourse/<%= dept._id%>/<%= s._id%>/course" method="get">
    <button type="submit">courses</button>
 </form>
        </li>
      <% }) %>
    </ul>
   
  <% } else { %>
    <p>No students found</p>
  <% } %>
<% } %>

</body>
</html>